# Conformance

Every language SDK will run the same semantic scenarios from `spec/fixtures/`. A conforming implementation must agree on validation, request serialization, provider-state preservation, errors, timeout certainty, and webhook verification while presenting an idiomatic interface for its language.
